.venv/
__pycache__/
tools/
data/
*.duckdb
*.parquet

# Prior Labs licence token. Never commit; inject as TABPFN_TOKEN on pods.
token.txt

# Golden vectors are conformance fixtures for the C++ extension — they ship with the repo.
!reference/golden/*.parquet
